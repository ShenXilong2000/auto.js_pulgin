toast("开始运行，跳过广告");
while(true){
    while(text("跳过 4").exists())
    {
        text("跳过 4").findOne().click();
    }
    while(text("跳过广告 4").exists())
    {
        text("跳过广告 4").findOne().click();
    }
    while(text("跳过4").exists())
    {
        text("跳过4").findOne().click();
    }
    while(text("跳过 3").exists())
    {
        text("跳过 3").findOne().click();
    }
    while(text("点击跳过4").exists())
    {
        text("点击跳过4").findOne().click();
    }
    while(text("跳过3").exists())
    {
        text("跳过3").findOne().click();
    }
    while(text("3 跳过广告").exists())
    {
        text("3 跳过广告").findOne().click();
    }
    while(text("跳过").exists())
    {
        text("跳过").findOne().click();
    }
    while(text("点击跳过 5").exists())
    {
        text("点击跳过 5").findOne().click();
    }
    while(text("跳过 5").exists())
    {
        text("跳过 5").findOne().click();
    }
    while(text("跳转 3s").exists())
    {
        text("跳转 3s").findOne().click();
    }
    while(text("跳过2s").exists())
    {
        text("跳过2s").findOne().click();
    }
    while(text(" 跳过").exists())
    {
        text(" 跳过").findOne().click();
    }
    while(text("跳过 >").exists())
    {
        text("跳过 >").findOne().click();
    }
    while(text(" 跳转 3s ").exists())
    {
        text(" 跳转 3s ").findOne().click();
    }
}